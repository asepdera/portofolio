import { Tooltip } from "@chakra-ui/react"
import { SearchIcon } from "@chakra-ui/icons"

const Tooltips = () => {
    return (
        <Tooltip hasArrow label="coba" >
            <SearchIcon />
        </Tooltip>
    )
}

export default Tooltips